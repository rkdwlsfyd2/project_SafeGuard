# 🗂️ Card Components

이 디렉토리는 대시보드와 마이페이지에서 데이터 요약(KPI) 정보를 시각화하는 카드 형태의 컴포넌트들을 포함합니다.

## 📄 주요 컴포넌트

### 1. CardDataStats.tsx
데이터 포인트의 수치, 증감률, 제목을 표시하는 다목적 통계 카드입니다.

#### Props
| Prop | Type | Description |
| :--- | :--- | :--- |
| `title` | `string` | 카드의 하단에 표시될 통계 항목의 이름 (예: "전체 민원") |
| `total` | `string` | 표시할 주 수치 데이터 |
| `rate` | `string` | 증감 수치 또는 비율 (예: "0.43%") |
| `levelUp` | `boolean` | `true` 이면 상승(녹색/위쪽 화살표), `false` 이면 하락(빨간색/아래쪽 화살표) |
| `children` | `ReactNode` | 카드 좌상단에 표시될 아이콘 컴포넌트 (`lucide-react` 권장) |

#### 사용 예시
```tsx
<CardDataStats
  title="Total views"
  total="$3.456K"
  rate="0.43%"
  levelUp
>
  <EyeIcon size={22} className="text-blue-600" />
</CardDataStats>
```

---
> [!NOTE]
> 이 컴포넌트들은 **TailAdmin** 디자인 가이드를 준수하며, `index.css`에 정의된 테일윈드 유틸리티 클래스를 사용하여 반응형 레이아웃과 다크모드를 지원합니다.
